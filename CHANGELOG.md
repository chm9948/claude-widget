# 변경 내역 (Changelog)

이 파일은 `claude-widget`의 버전별 변경 사항을 기록합니다.
버전 번호는 소스의 `$script:appVersion` 및 ⓘ 정보 패널의 "버전" 값과 일치시킵니다.

형식: [Keep a Changelog](https://keepachangelog.com/) 약식 · 날짜는 KST 기준.

## [v1.6.0] - 2026-06-30

### Changed
- 업데이트 방식 변경 — ⓘ 패널의 새 버전 링크 클릭 시 브라우저 다운로드 대신 **setup 스크립트를 자동 실행**(원클릭 업데이트). 최신 exe를 재설치하고 위젯을 재시작한다. 문구도 "vX.Y.Z 로 업데이트"로 변경.

### Added
- 경량 설치 스크립트 `setup.ps1` — `iex (irm ...)` 한 줄로 설치/제거(`%LOCALAPPDATA%`, 차단 해제, 시작메뉴 바로가기).

## [v1.5.0] - 2026-06-29

### Added
- Windows 시작 시 자동 실행 — 트레이 우클릭 메뉴에서 토글(체크). 레지스트리 `HKCU\...\Run`에 등록/해제.
- 최소화(한 줄) 화면에 현재 블록 사용률 % 표시 (금액 옆).
- 새 버전이 있으면 트레이 아이콘에도 빨간 점 표시.

### Changed
- X 버튼 동작: 종료 → **트레이로 숨김**. 완전 종료는 트레이 메뉴 "종료".

## [v1.4.0] - 2026-06-29

### Added
- 시스템 트레이 아이콘 — 트레이에 상주. 좌클릭으로 위젯 표시/숨김 토글, 우클릭 메뉴(열기/숨기기/종료). 작업표시줄에는 표시하지 않음(`ShowInTaskbar=False`).
- 전용 앱 아이콘(`claude-widget.ico`) — 인디고 라운드 사각형 + "$". exe·트레이·작업표시줄에 적용.

## [v1.3.1] - 2026-06-26

### Changed
- ⓘ 패널 이슈 링크 문구에 "개선" 추가: "버그 신고·건의" → "버그 신고·개선·건의".

## [v1.3.0] - 2026-06-26

### Added
- 업데이트 확인 — 저장소 CHANGELOG 최상단 버전을 1시간 주기(시작 시 1회)로 읽어 현재 버전과 비교. 새 버전이 있을 때만 ⓘ 패널에 "최신 vX.Y.Z 다운로드" 링크 표시(클릭 시 main의 `claude-widget.exe` 다운로드). 최신이면 버전 옆에 "(최신)" 표시.
- 새 버전 알림 red-dot — 새 버전이 있으면 헤더 ⓘ 아이콘과 다운로드 링크에 빨간 점 표시.

### Changed
- 헤더 버튼 순서 변경: 최소화(─) ↔ 테마(☾/☀) 위치 교체.

## [v1.2.0] - 2026-06-26

### Added
- 최소화 기능 — 헤더의 ─ 버튼을 누르면 금액만 한 줄로 축소(너비도 좁아짐), ▢ 버튼으로 전체 복원. 최소화 상태에서도 복원·닫기 가능.

## [v1.1.0] - 2026-06-26

### Added
- ⓘ 정보 패널에 GitHub 이슈 링크("버그 신고·건의") 추가 — 클릭 시 기본 브라우저로 저장소 이슈 페이지 열림.

## [v1.0.0] - 2026-06-26

이번 버전부터 변경 내역을 문서로 관리하기 시작합니다. 현재 기능 기준선입니다.

### 데이터 소스
- 빌링 블록 종료 시각·사용률을 Anthropic `oauth/usage` API의 `five_hour`에서 직접 조회 (`/usage`와 동일한 값, 모든 클라이언트 사용량 포함).
- 월 누적 비용·모델별 내역은 `ccusage`(월간/블록) 유지.
- API 실패 시 마지막 성공값을 캐시로 표시하고, 429(rate limit)는 15분·기타 오류는 5분 백오프. 첫 성공 전까지만 ccusage 폴백 공식 사용.

### 동작
- 자동 갱신 주기 60초 → **5분**.
- 푸터 카운트다운을 분 단위로 표시 ("4분 47초 후 갱신").

### UI
- 좌측 상단 AI 선택 콤보박스 제거 → "CLAUDE CODE" 고정 라벨 (Claude Code 전용).
- 헤더에 ⓘ 버튼 추가 → 클릭 시 만든이 정보 패널 펼침/접힘 (문의 메일, 버전).
- 푸터 3컬럼 재구성(슬라이더 64px · 갱신 텍스트 가변폭 · 새로고침)으로 텍스트 잘림 해결.

### 버그 수정
- 잔여 시간 표시의 `[int]` 반올림 오류(분 ≥ 30 시 1시간 더 표시) → `[math]::Floor` 적용.

### 문서
- `README.md` 추가 — 위젯 설명, 실행/요구사항, 데이터 소스, 빌드 방법.
- `CHANGELOG.md` 도입 — 버전별 변경 내역 관리 시작.

<!--
다음 버전 추가 시 형식 예시:

## [v1.1.0] - YYYY-MM-DD

### Added
- 새 기능

### Changed
- 변경

### Fixed
- 수정
-->
